PlayerStep = 5;

//Every {time value} steps an encounter will always appear
//Min amount of steps to get encounter variates for enemy(in this case one enemy)
EncounterTimer = 80;
EncounterTimerMin = 20;
EncounterCurrent = 0;

//Random
RandomNumber = 0;

//Keyboard inputs
//up = keyboard_check(vk_up);
//down = keyboard_check(vk_down);
//left = keyboard_check(vk_left);
//right = keyboard_check(vk_right);

//Un- predefined variables

